import moment from "../pages/dob/dob";


export default function FormatDate(){
// return    moment(selectedYear).format('YYYY') + '-' + moment(selectedMonth).format('MM')  + '-' + moment(selectedDate).format('DD');
}
